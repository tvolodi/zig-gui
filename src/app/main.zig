// src/app/main.zig
// Entry point for the zig-gui app. Real implementation is in App.run().
// This file is a placeholder until the renderer (module 09) is complete.
const std = @import("std");

pub fn main() !void {
    // TODO: wire App.run() once module 09 (renderer) is complete
    std.log.info("zig-gui: no runnable app yet — renderer (module 09) pending", .{});
}
