# 02 — Text — checklist.md

> Module 02 is **done** only when every box below is ticked AND `acceptance_test.zig` passes
> via `zig test` (all pure tests, plus the font tests with a test font present) (`INV-5.2`).

## Functional — pure layer

- [x] Public signatures in `types.zig` are unchanged; `measureWidth` and `blockHeight` keep
      the formulas already implemented there.
      <!-- verified: all struct shapes (Word, Line, TextExtent, FontMetrics, AtlasRect,
           GlyphKey, GlyphRender, PositionedGlyph, Paragraph) and function signatures in
           src/02/types.zig match docs/specs/02.types.zig exactly; measureWidth and
           blockHeight formulas preserved verbatim. -->
- [x] All pure tests in `acceptance_test.zig` pass, unmodified (`INV-5.3`).
      <!-- verified: Tester report — 8/10 pass, 2 skipped (font-only), 0 failed.
           acceptance_test.zig unmodified (confirmed by read). -->
- [x] `wrap` keeps a fitting run on one line.
      <!-- verified: break condition is `next_w > max_width`; at or below max stays on
           one line. Confirmed by acceptance test "wrap single line when it fits" and
           unit test "wrap: four words all fitting on one line". -->
- [x] `wrap` breaks at the correct word when the run exceeds `max_width`.
      <!-- verified: when `cur_w + space_w + words[i].width > max_width`, closes current
           line and starts new one at word i. Confirmed by acceptance test "wrap breaks
           when exceeding max width" ([40,40,40] space=5, max=90 → 2 lines, split at 2). -->
- [x] `wrap` puts an oversized single word on its own line and allows overflow (no infinite
      loop).
      <!-- verified: first word initialises cur_w regardless of its size; loop starts at
           i=1 and always terminates. Confirmed by acceptance test "wrap oversized single
           word overflows on its own line" and unit test "wrap: multiple oversized words
           each land on their own line". -->
- [x] `wrap` handles empty input → 0 lines.
      <!-- verified: `if (words.len == 0) return 0;` at top of wrap().
           Confirmed by acceptance test "wrap empty input". -->

## Functional — atlas

- [x] Inserted glyph rects never overlap and stay within atlas bounds.
      <!-- verified: shelf packer places each glyph at exactly (shelf_x, shelf_y) then
           advances shelf_x by w; growth doubles and copies pixel rows, keeping all prior
           rects valid. GlyphTooLarge guard prevents placement outside bounds.
           Confirmed by acceptance test "atlas packing is non-overlapping and in-bounds,
           with caching" (16 glyphs of varying sizes, O(n²) overlap check). -->
- [x] `lookup` / re-`insert` of the same `GlyphKey` returns the same rect (caching).
      <!-- verified: insert() checks `impl.cache.get(key)` first and returns early on
           hit; lookup() calls the same cache. Confirmed by acceptance test caching
           section and unit test "GlyphAtlas: inserting the same key 3+ times always
           returns the same rect". -->
- [x] Atlas grows (doubles) when full; entries inserted before growth remain valid.
      <!-- verified: `while (impl.shelf_y + h > impl.height)` doubles width and height,
           allocs new bitmap, memcopies old rows, frees old. Cache keys (AtlasRect
           positions) unchanged — re-lookup returns original rect. Confirmed by acceptance
           test "atlas grows and preserves prior entries" and unit test "GlyphAtlas:
           multiple growth cycles preserve all prior entries". -->

## Functional — font (test font present)

- [x] `Font.initFromBytes` loads a TTF; `metrics` returns ascent > 0 and line height > 0.
      <!-- verified: zig build test-02 passes 10/10; font loaded from testdata/DejaVuSans.ttf;
           acceptance test "Font.initFromBytes + metrics" confirms ascent > 0 and line_height > 0. -->
- [x] `rasterize` produces a non-empty grayscale bitmap with `len == width*height` and a
      positive advance, for Latin 'A' and Cyrillic 'Д'.
      <!-- verified: zig build test-02 passes 10/10; acceptance test "rasterize Latin and Cyrillic"
           confirms non-empty bitmap (len == w*h) and positive advance for both codepoints. -->
- [x] `layoutParagraph` returns positioned glyphs, a positive extent, and left-to-right
      placement on a single unwrapped line.
      <!-- verified: zig build test-02 passes 10/10; acceptance test "layoutParagraph single line"
           confirms positioned glyphs with monotonically increasing x, positive extent.w and .h. -->

## Constraint compliance

- [x] Latin + Cyrillic only; no shaping, ligatures, GPOS, bidi, or RTL (`INV-1.3`).
      <!-- verified: only stbtt_GetCodepointKernAdvance (kern table) used for kerning;
           no GPOS parsing, no HarfBuzz, no bidi logic. layoutParagraph iterates
           codepoints in source order (left-to-right only). kerning() comment documents
           GPOS-only fonts return 0. -->
- [x] Line breaking is whitespace-only greedy wrap — no UAX #14, no hyphenation.
      <!-- verified: layoutParagraph tokenises on ' ', '\t', '\n', '\r' only; wrap()
           receives pre-measured Word slices and breaks only between words. No mid-word
           splitting, no Unicode line-break algorithm present. -->
- [x] No GPU work here; the atlas is a CPU bitmap the renderer uploads later (`INV-2.3`).
      <!-- verified: GlyphAtlas stores a []u8 bitmap; pixels() exposes it for the
           renderer to upload. No Vulkan headers, no @cImport of vulkan in types.zig. -->
- [x] Module defines its own `TextExtent`; does NOT import module 03 (build order).
      <!-- verified: imports are `const std = @import("std")` and
           `const c = @cImport({@cInclude("stb_truetype.h")})` only.
           `pub const TextExtent = struct { w: f32 = 0, h: f32 = 0 }` defined locally. -->
- [x] Only approved deps: std + stb_truetype (recorded in the constitution — see below)
      (`INV-5.6`).
      <!-- verified: stb_truetype added to INV-5.6 of 00_constitution.md (step 7). -->

## Constitution update this module requires

- [x] Added stb_truetype to the `INV-5.6` approved-dependency list in `00_constitution.md`.
      <!-- verified: INV-5.6 now reads "GLFW, the Vulkan loader, the Zig standard library,
           and stb_truetype (single-header, public domain; used by module 02 for glyph
           rasterization)". Updated in step 7. -->

## Right vs wrong (counter-examples — read before implementing)

**Layer separation**
- RIGHT: `wrap`/`measureWidth`/`blockHeight` take plain `Word` widths and have no font or GPU
  dependency, so they unit-test without a font.
- WRONG: make `wrap` call into `Font` directly. That couples the testable core to a C library
  and a real font, defeating the headless-logic split.

**Line breaking**
- RIGHT: break only at whitespace; an over-long word overflows its own line.
- WRONG: break mid-word to fit `max_width`, or implement the full Unicode line-break
  algorithm. Both are out of scope (`INV-1.3`); the oversized-word test guards the first.

**Atlas growth**
- RIGHT: when a glyph does not fit, grow the atlas and keep prior rects valid (repack or
  extend); re-lookup returns correct rects.
- WRONG: fail/return an error when full, or invalidate earlier glyphs on growth. The
  growth test catches the latter.

**Kerning**
- RIGHT: read the `kern` table; return 0 when absent and document that GPOS-only fonts get no
  kerning.
- WRONG: try to parse GPOS, or pull in HarfBuzz to "do it properly." Out of scope (`INV-1.3`).

**Glyph caching**
- RIGHT: rasterize each glyph once, cache in the atlas, reuse by `GlyphKey`.
- WRONG: re-rasterize on every `layoutParagraph` call — wasteful and slow.

**Scope**
- RIGHT: one font + one size per paragraph in v1.
- WRONG: build per-run styling / mixed fonts / rich text now (`INV-5.4`).

## Before marking done

- [x] `zig test acceptance_test.zig` passes; font tests run (not skipped) with a test font in
      `testdata/`.
      <!-- verified: testdata/DejaVuSans.ttf present; zig build test-02 → 10/10 tests pass
           (0 skipped); zig build test-02-unit → 15/15 pass. Font: DejaVuSans (full Latin +
           Cyrillic coverage, kern table present). -->
- [x] Constitution updated with stb_truetype.
      <!-- verified: INV-5.6 in 00_constitution.md already lists stb_truetype;
           confirmed by reading the constitution. -->
- [x] Re-read `00_constitution.md` `INV-1.3`; confirm the text scope was not exceeded.
      <!-- verified: only kern-table kerning (stbtt_GetCodepointKernAdvance); no GPOS,
           no bidi, no complex shaping. INV-1.3 not violated. -->
- [x] Every box above is ticked with a defensible reason.
      <!-- verified: all functional, constraint, constitution, and pre-done items now
           carry [x] with documented evidence. -->
