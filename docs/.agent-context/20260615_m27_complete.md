M27 (Dashboard / data-product widgets) is complete. All seven requirements were implemented across modules 07, 09, 13, and the app layer: RN1 added donut-ring rendering (`inner_radius`, `center_label` slot) to the pie chart in `src/13/marks.zig` and `src/13/chart.zig`; RN2 added annotation callouts with leader lines (`Callout` struct, `renderCallouts`, `computeCalloutPos`); RN3 added the `DateRangePicker` widget kind to `src/07/types.zig` with `DateRangePickerState`, five presets, `setDateRange`, `getDateRange`, and `setDateRangePreset`; RN4 added `formatCurrency` to `src/app/locale.zig` for USD, SGD, and EUR with locale-aware grouping; RN5 added the `MaskableValue` widget kind with `MaskableValueState`, `setMaskableValue`, and `setMaskableVisible` (fixed-width masked display, default char `*`); RN6 added the `TrendBadge` widget kind with `TrendBadgeState`, `TrendDirection` enum, and `setTrendValue` (direction auto-computed from sign, colors sourced from tokens only per INV-4.3); RN7 added `CrosshairOptions`, `CrosshairState`, and `updateCrosshairX` to `src/13/interaction.zig` with dashed-polyline rendering via `marks.renderCrosshair`. Pre-existing issues resolved in the same pass: `docs/specs/02.acceptance_test.zig` updated `GlyphKey.codepoint` → `glyph_id` at 5 call sites; `src/08/types.zig` gained three missing switch cases for new widget kinds; `src/09/09_test.zig` added `polyline`, `filled_path`, and `arc` to its exhaustive switch; `src/13/marks.zig` fixed Zig 0.16 allocator-argument ordering and type mismatches in `ArrayListUnmanaged.append` calls. The build passes with 311 tests green. Documentation updated: `docs/ROADMAP.md` gained the M27 milestone table (marked `done`); `docs/HOW_TO_USE.md` already contained the full M27 usage section (added during implementation); `docs/AGENT_GUIDE.md` was updated with M27 patterns (donut ring geometry, dashed-line pattern, decoupled crosshair state, center-label slot, test arena pattern, `ArrayListUnmanaged.append` Zig 0.16 signature).
