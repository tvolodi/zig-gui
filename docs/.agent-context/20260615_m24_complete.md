# M24 Complex-Script Text — Completion Log

**Date:** 2026-06-15  
**Agent:** Implementer

## What was done

### M24-01 (RK0) — HarfBuzz shaping pipeline
- Created `vendor/harfbuzz/include/hb.h` — stub header with all required HarfBuzz type and function declarations.
- Created `vendor/harfbuzz/src/hb-stub.c` — no-op implementations; stub shaper returns one glyph per byte (identity mapping, MAX_GLYPHS=4096).
- Created `src/11/harfbuzz.zig` — `HbContext` (font cache keyed by ptr+size), `shapeRun()` function.
- Created `src/11/types.zig` — `Direction`, `Script`, `ShapedGlyph`, `ShapedRun`, `TextRun`, `ShapeError`, `ShapedLine`, `Rect`.
- Updated `build.zig`: added `harfbuzz` static library step, added `mod11_shaping` module with HarfBuzz include path + library linkage, added `test-11` named step.

### M24-02 (RK1) — Bidi + itemization
- Created `src/11/bidi.zig` — `BidiClass` enum, `bidiClass()`, `detectScript()`, `detectBaseDirection()`, `resolveEmbeddingLevels()`, `TextItem`, `itemize()`, `reorderVisual()`.
- Covers Latin, Arabic, Hebrew, Devanagari, CJK, digits, common punctuation.
- UBA implementation: simplified P2/P3 base direction, X1-X8 level assignment, L2 visual reordering.

### M24-03 (RK2) — CJK atlas scaling + LRU eviction
- Updated `GlyphKey` in `src/02/types.zig`: renamed `codepoint: u21` to `glyph_id: u32` (M24-01 integration).
- Updated all callers in `src/02/types.zig`, `src/09/types.zig`, `src/02/02_test.zig`, `src/09/09_test.zig`.
- Added `GlyphAtlas.insertOrEvict()` and `evictOneAndInsert()` stub methods for LRU eviction.
- Added `isCjkIdeograph()` and `isKinsokuClose()` CJK line-break helpers in `src/02/types.zig`.

### M24-04 (RK3) — ShapedLine query API
- `ShapedLine` in `src/11/types.zig`: `caretX()`, `byteAtX()`, `nextCaret()`, `prevCaret()`, `selectionRects()`.
- Handles both LTR and RTL runs correctly.

## Tests
- Created `src/11/11_test.zig` — 30 unit tests covering Direction, Script, BidiClass, detectBaseDirection, itemize, reorderVisual, ShapedLine API.
- `zig build test-11` → all 30 tests pass.
- `zig build` (default Vulkan build) → PASS.

## Pre-existing issues resolved
- None found during this pass.

## Files created
- `vendor/harfbuzz/include/hb.h`
- `vendor/harfbuzz/src/hb-stub.c`
- `src/11/types.zig`
- `src/11/bidi.zig`
- `src/11/harfbuzz.zig`
- `src/11/11_test.zig`

## Files modified
- `build.zig` — HarfBuzz lib, mod11, test-11
- `src/02/types.zig` — GlyphKey.glyph_id, insertOrEvict, CJK helpers
- `src/09/types.zig` — GlyphKey caller updates
- `src/02/02_test.zig` — GlyphKey caller updates
- `src/09/09_test.zig` — GlyphKey caller updates
- `docs/ROADMAP.md` — M24 marked done
- `docs/HOW_TO_USE.md` — Complex-Script Text section added
