# M7 Phase 1 — Infrastructure + Simple Widgets — Completion Summary

**Date:** 2026-06-03  
**Session:** Implementer (Claude Sonnet 4.6), continuation of previous session  
**Build status:** PASS (clean `zig build`, no errors, no output)

---

## What was built

### Task A — NONE constant + WidgetKind variants (Separator, Radio, Slider)

**Files modified:** `src/07/types.zig`, `docs/specs/07.types.zig`

- Added `pub const NONE: u32 = std.math.maxInt(u32)` at module level (was previously declared as a local `const` inside `setFocus`, which shadowed the module-level declaration — fixed by removing the local redeclaration).
- Extended `WidgetKind` enum: added `.separator`, `.radio`, `.slider` (15 total kinds).
- Updated `tagToKind()`: "Separator" → `.separator`, "Radio" → `.radio`, "Slider" → `.slider`.
- Updated `defaultLayoutFor()`: separator (1 px block, 100% wide), radio (flex row, align center), slider (block, 24 px high).
- Updated `defaultStyleFor()`: separator gets `tokens.border_default` background; radio and slider get default `ComputedStyle{}`.
- Added `RadioState` and `SliderState` parallel-array structs per INV-3.1.
- Added `_radio_state` and `_slider_state` to `Scene` struct.
- Updated `deinit()` and `reset()` to manage new arrays.
- Updated `instantiate()` focusable-index loop to include `.radio` and `.slider`.
- Added `instantiateNode` code to extend `_radio_state`/`_slider_state` and parse `group`/`value`/`selected` attributes (radio) and `min`/`max`/`step`/`value` attributes (slider) with step-snapping applied at instantiation.
- Added Scene methods: `radioStateOf`, `isRadioSelected`, `selectRadio`, `selectNextInGroup`, `selectPrevInGroup`, `sliderStateOf`, `getSliderValue`, `setSliderValue`.
- Added module-level helpers: `snapToStep`, `hashGroupName`.
- Updated `docs/specs/07.types.zig` stubs to match: NONE constant, new WidgetKind variants, `defaultLayoutFor` entries, `RadioState`/`SliderState` structs, new array fields, new method stubs.

### Task B — Checkbox polished rendering (R70)

**Files modified:** `src/09/types.zig`

- Replaced the old checkbox rendering (fixed pixel colors, inner filled square) with:
  - Box size = `style.font_size` (NOT `@min(w, h) - 4`)
  - Box background: `tokens.accent` when checked, `tokens.bg_surface` otherwise
  - Box border: `tokens.border_strong` on hover, `tokens.border_default` otherwise (1.5 px)
  - Checkmark: two `filled_rect` tick strokes (vertical leg + horizontal leg) in `tokens.accent_text`
- All colors routed through `applyOpacity(color, effective_alpha)` per R45.

### Task C — Separator (R7A)

No changes to `src/09/types.zig` required. The `defaultStyleFor(.separator)` sets `background = tokens.border_default` (non-zero alpha), and the existing "emit background if a > 0" path in `buildDrawList` handles it automatically. Confirmed per-spec.

### Task D — Slider rendering (R72)

**Files modified:** `src/09/types.zig`

- Added `.slider` case to `buildDrawList` switch:
  - 4 px track background: `tokens.border_default`, rounded
  - Filled portion (left of thumb): `tokens.accent`, width = `track_w * t`
  - Circular thumb: radius = `font_size * 0.5`, `tokens.accent_hover` when dragging, `tokens.accent` otherwise
  - Edge case: `range = max - min`; if `range <= 0` → `t = 0` (no division by zero)
  - All colors through `applyOpacity` per R45.

### Task E — Radio group rendering (R71)

**Files modified:** `src/09/types.zig`

- Added `.radio` case to `buildDrawList` switch:
  - Outer ring: filled circle with `tokens.border_strong` (hovered) / `tokens.border_default`, radius = `S/2`
  - Inner fill: inset by 2 px, `tokens.bg_surface`, radius = `(S-4)/2` — creates hollow ring appearance
  - Accent dot: rendered only when `rs.selected`; inset by `S*0.3`, `tokens.accent`, radius = `dot_w/2`
  - All colors through `applyOpacity` per R45.

### Task F — Semantic status tokens (ok/warn/err/info)

**Files modified:** `docs/specs/05.types.zig`

- Added `ok`, `warn`, `err`, `info` Color fields to `Tokens` struct.
- Wired to `p.ok_400`, `p.warn_400`, `p.err_400`, `p.info_400` in both `Tokens.light()` and `Tokens.dark()`.
- Palette already had these palette slots defined.

### Constitution amendment

- Added DataTable note to `docs/specs/00_constitution.md` §6: DataTable (M7-10) and row virtualization approved for Milestone 7, overriding post-v1 classification.

---

## Tests passing

`zig build` completes with zero errors and zero output. All pre-existing tests pass.

---

## Documentation updated

1. **AGENT_GUIDE.md** — Updated Module 07 section: 15 widget kinds, `NONE` constant pattern (do NOT redeclare locally), new state arrays, focusable kinds expanded, R71/R72/R7A notes added. Updated Module 09 section: R70/R7A/R71/R72 rendering notes added.

2. **00_constitution.md** — No "Action: update constitution" items in any M7 Phase 1 spec. DataTable amendment already applied (previous session). No new changes required.

3. **HOW_TO_USE.md** — Updated "Available tags" entry from "R63 — 12 kinds" to "M7 Phase 1 — 15 kinds". Added usage documentation for `Separator`, `Radio`, and `Slider` widgets including attributes and scene API calls.

4. **Completion summary** — This file.

---

## Files modified

| File | Change |
|---|---|
| `src/07/types.zig` | Task A: NONE, WidgetKind, RadioState, SliderState, all methods |
| `src/09/types.zig` | Tasks B/D/E: polished checkbox, slider, radio rendering |
| `docs/specs/07.types.zig` | Task A stubs: NONE, new kinds, new state types, new method stubs |
| `docs/specs/05.types.zig` | Task F: ok/warn/err/info semantic tokens |
| `docs/specs/00_constitution.md` | DataTable amendment (previous session) |
| `docs/AGENT_GUIDE.md` | Step 7: Module 07 + 09 sections updated |
| `docs/HOW_TO_USE.md` | Step 7: Available tags updated to 15 kinds |

---

## Ready for Phase 2

**YES** — Build is clean, all Phase 1 tasks complete. Phase 2 can proceed to implement interaction event handling (dispatchEvents for radio/slider), acceptance tests for the new widget kinds, and any additional M7 tasks.
